<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.votingapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VotingApp"
        android:usesCleartextTraffic="true">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".MainActivity">
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" ></activity>
        <activity android:name=".Home" ></activity>
        <activity android:name=".CreatePoll" ></activity>
        <activity android:name=".JoinPoll" ></activity>
        <activity android:name=".Voting" ></activity>
        <activity android:name=".Results" ></activity>
        <activity android:name=".AlternativeVoteVoting"></activity>
        <activity android:name=".AlternativeVoteResult"></activity>
        <activity android:name=".PollingMethodsExplained"></activity>
        <activity android:name=".AllPolls"></activity>
    </application>

</manifest>